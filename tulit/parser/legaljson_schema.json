{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "LegalJSON",
    "type": "object",
    "properties": {
        "preface": {
            "type": [
                "string",
                "null"
            ]
        },
        "preamble": {
            "type": "object",
            "description": "Structured preamble: opening formula, citations, recitals (with optional intro) and the final formula ('HAS ADOPTED THIS REGULATION:').",
            "properties": {
                "formula": {
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "citations": {
                    "type": "array",
                    "items": {
                        "type": [
                            "object",
                            "string"
                        ]
                    }
                },
                "recitals_intro": {
                    "type": [
                        "string",
                        "null"
                    ]
                },
                "recitals": {
                    "type": "array",
                    "items": {
                        "type": [
                            "object",
                            "string"
                        ]
                    }
                },
                "final": {
                    "type": [
                        "string",
                        "null"
                    ]
                }
            },
            "required": [
                "citations",
                "recitals"
            ]
        },
        "chapters": {
            "type": "array",
            "items": {
                "type": [
                    "object",
                    "string"
                ]
            },
            "description": "Divisions above the article level (part/title/chapter/section/subsection), typed and ELI/AKN-named (part_, title_, chp_, sec_, subsec_), each with a parent link to its enclosing division."
        },
        "articles": {
            "type": "array",
            "items": {
                "type": "object"
            },
            "description": "Articles of the legal act, in document order. Each article carries eId (art_N), num, heading, parent (eId of the nearest enclosing division from 'chapters', or null when directly under the enacting terms) and children (NNN.NNN blocks with text and the structured 'amendment' field). Together with the parent links in 'chapters', this encodes the full enacting-terms hierarchy as a flat, navigable tree."
        },
        "conclusions": {
            "type": [
                "object",
                "null"
            ]
        },
        "annexes": {
            "type": "array",
            "description": "Annexes of the legal act, uniform with articles: eId, num, heading and children. Each child has eId (NNN.NNN), text (full readable text), and amendment — null for ordinary content, or a structured object {action: replace|insert|add|delete|amend|null, instruction, amended_act, quoted: [{kind: structure, children: [...]} | {kind: text, text}]} modelled on Akoma Ntoso mod/quotedText/quotedStructure. Tables carry a 'table' key with caption, rows and notes.",
            "items": {
                "type": "object"
            }
        },
        "structure": {
            "type": "array",
            "description": "Nested skeleton of the enacting terms, in document order: division nodes {eId, type, num, children} with article leaves {eId, type: 'article'}. References only — content lives in the flat 'chapters' and 'articles' lists (hybrid flat+tree representation)."
        }
    },
    "required": [
        "preface",
        "preamble",
        "articles"
    ]
}